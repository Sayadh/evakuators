/**
 * Mock API data — ONLY tow trucks live here.
 * This is the entity that will come from the real backend API
 * (driver registrations and profiles).
 *
 * Static geography (regions, cities, districts) lives in /data
 * and is NOT replaced by the API.
 */
export { mockTowTrucks } from './towTrucks'
